//! `clewdr menu` — interactive TUI wrapping the rest of the verbs.

use crate::error::ClewdrError;

pub async fn run() -> Result<(), ClewdrError> {
    unimplemented!("menu is implemented in commit #11")
}
